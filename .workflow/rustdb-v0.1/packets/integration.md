# Packet: integration

Ownership: crate manifest, public API, engine/session integration, CLI, tests,
documentation, container development environment, and benchmark harness.

Integrate all implementation packets behind the stable public API, keep
internal plan/operator types private, and run the final OrbStack and MinIO
quality gates.
