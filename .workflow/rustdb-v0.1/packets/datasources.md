# Packet: datasources

Ownership: `src/storage/**` and `src/datasource/**` only.

Implement local/S3 location resolution plus strict streaming CSV and range-aware Parquet reading. Expose small internal APIs for registration and scans. Do not edit manifests or engine/SQL modules.
